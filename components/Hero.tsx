/**
 * Legacy Hero — replaced by CinematicHero. Kept as re-export for any
 * downstream imports that may still reference it.
 */
import CinematicHero from "./CinematicHero";
export default CinematicHero;
